namespace DO;

public enum paymentOptions  ///enum for payment options.
{
    CreditCard,
    Cash,
    Check,
    Bit
};

public enum categories   ///enum for categories.
{
    watchs,
    cameras,
    camputers,
    ipads 
};